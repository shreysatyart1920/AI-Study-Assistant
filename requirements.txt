streamlit>=1.30.0
PyPDF2>=3.0.0
groq>=0.5.0
python-dotenv>=1.0.0
numpy>=1.24.0

# NEW: Optional but recommended for semantic chunk retrieval.
# If install fails, app automatically falls back to keyword search.
sentence-transformers>=2.2.0
